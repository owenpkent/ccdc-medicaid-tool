node_modules/
dist/
*.tsbuildinfo
.vite/
coverage/

# Vendored runtime assets (tesseract worker, WASM cores, language data, the
# zxing barcode reader, and pdf.js's standard PDF fonts). ~40 MB of binaries,
# reproducibly copied from node_modules by scripts/vendor-ocr.mjs (run
# automatically on predev/prebuild). Not committed.
public/vendor/
